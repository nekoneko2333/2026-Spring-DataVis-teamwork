# 621 MCF README

更新日期：2026-06-21

## 本轮主要目标

这轮主要围绕首页生成短片、体渲染视觉风格、二维图表配色、播放/交互性能和固定视频方案做了多次迭代。最终决定：不再使用导出的 MP4 短片，首页短片回到实时体渲染方式。

## 已完成的改动

1. 首页短片逻辑
   - 曾尝试把短片导出为 `public/videos/cosmic-web-story.mp4` 并优先播放固定视频。
   - 后来确认该离线 MP4 与项目原始 Three.js 体渲染方式视觉不一致，已取消该方案。
   - 当前首页点击“播放生成短片”会直接调用实时渲染 `_startStory()`，不再创建 `<video>`，也不再请求 MP4/WebM。
   - 删除了固定视频文件和导出脚本入口。

2. 播放期间交互
   - 首页停留状态和短片播放期间禁用视角控制，避免实时渲染时旋转视角造成卡顿。
   - 进入探索模式或退出短片后恢复视角控制。
   - 已验证：播放期间 controls 为 `false`，退出/探索后 controls 为 `true`。

3. 首页视觉风格
   - 保持黑白灰风格，接近参考网站的克制视觉。
   - 删除金色按钮和强彩色装饰。
   - 首页按钮改为白色文字 + 细横线，不再使用方框按钮。
   - 首页文字缩小、减重、靠边放置。

4. 体渲染风格
   - 体渲染保持黑白/灰阶，不再使用彩色 transfer function。
   - MIP 模式做了动态范围压缩，避免整体曝光成白屏。
   - 传递函数默认 fallback 也改成灰阶，避免主题未加载时闪绿色。

5. 二维图表配色
   - 只在需要区分指标的二维图上保留克制颜色。
   - 统计曲线保留低饱和色区分不同指标。
   - 功率谱恢复低饱和彩色线：蓝灰、青灰、米灰。
   - 指纹图扫描线改为白灰，不再绿色。
   - 传递函数线改为白灰，不再绿色。
   - 直方图低密度端最低亮度提高，避免左侧融进黑底。

6. 拓扑/网络层
   - 拓扑 overlay 可以显隐。
   - 首页和短片前期默认弱化或隐藏拓扑，避免解释阶段画面太乱。
   - 切换网络模型时会重新显示拓扑。

7. 进程和端口
   - 当前期望只保留一个本地服务。
   - 最近验证时服务在 `http://127.0.0.1:5173/`。

## 当前重要文件

- `src/main.js`
  - 首页短片入口、实时 story 流程、拓扑显隐、播放控制、页面切换逻辑。

- `src/styles.css`
  - 首页黑白风格、按钮样式、图表/体渲染的全局视觉覆盖。

- `src/themes.js`
  - 图表和体渲染主题色。体渲染为灰阶，二维图表保留低饱和指标色。

- `src/visualization/shaders.js`
  - MIP 亮度压缩和体渲染 shader 调整。

- `src/visualization/VolumeRenderer.js`
  - controls 启停、网络层显隐、渲染器状态。

- `src/charts/Histogram.js`
  - 直方图低端灰阶最低亮度。

- `src/charts/Fingerprint.js`
  - 指纹图色阶和扫描线颜色。

- `src/charts/PowerSpectrum.js`
  - 功率谱使用 theme.power 恢复克制彩色。

## 当前还需要 debug 的问题

1. 首页短片中的“片层坍缩”仍然基本看不到
   - 用户反馈：进入首页短片后，片层坍缩阶段仍然不够明显。
   - 目前已尝试提高实时 story 中 sheet 阶段 density：
     - `src/main.js` 中 STAGE 02 / SHEETS 的 `density` 已调到 `1.16`。
   - 仍需进一步检查：
     - sheet brush 的区间是否合适：当前为 `25% - 75%` 密度百分位。
     - shader/transfer function 在灰阶下是否把中密度片层压得太暗。
     - `renderer.setBrush(true, mn, mx)` 在 story 播放时是否实际生效。
     - story 播放时使用预览体数据还是缓存体数据，是否造成片层细节不足。
     - 是否需要为 sheet 阶段单独使用更高 alpha 或更低 lo clip。

2. 进入首页后，时间控制的播放按钮点了不能播放
   - 用户反馈：进首页之后点时间控制播放不了。
   - 当前判断可能与 `story-mode` / `cinema-home` 状态有关：
     - 首页状态会调用 `renderer.setControlsEnabled(false)`。
     - 首页状态可能覆盖了普通时间轴 UI 或让部分交互处于隐藏/禁用状态。
     - `togglePlay()` 本身此前修过，但需要确认在 `cinema-home` 下点击的是哪个播放按钮、事件是否触发、`state.playing` 是否变化。
   - 需要进一步检查：
     - 首页状态下是否应该隐藏普通时间控制，还是允许它播放。
     - `#btnPlay` 点击后是否进入 `play()`。
     - `state.story.running` 或 `story-mode` 是否阻断了 `applyStep()` / 图表更新 / renderer 更新。
     - 若用户点击的是左侧时间控制，需要确认首页布局中该控件是否可见且没有被 overlay 截获。

## 最近验证过的事项

- 页面已不再创建 `<video>` 元素。
- 点击首页“播放生成短片”后进入 `cinematic-overlay ... running` 状态。
- 播放短片时不进入 `video-playing` 状态。
- 播放期间视角控制关闭。
- 退出短片或进入探索后视角控制恢复。
- `npm run build` 已通过。

## 后续建议 debug 顺序

1. 先修“首页时间播放按钮不能播放”
   - 这是交互 bug，范围应该比视觉 bug 更清晰。
   - 建议用浏览器脚本监听点击前后：
     - `state.playing`
     - `state.step`
     - `#btnPlay.textContent`
     - `renderer.controls.enabled`
     - `#cinematicOverlay.className`

2. 再修“片层坍缩看不见”
   - 建议先在 story 第 2 阶段暂停/截图。
   - 对比三组设置：
     - 当前灰阶 TF + sheet brush。
     - 提高 sheet alpha。
     - 改 sheet brush 到 `50% - 90%` 或 `60% - 95%`。
   - 不建议回到彩色体渲染；用户明确要求体渲染保持黑白风格。

## 注意事项

- 不要再恢复 MP4 固定短片方案，用户已明确要求不要放视频。
- 首页和体渲染保持黑白灰；只在二维图表需要区分指标时使用克制颜色。
- 不要为了提高片层可见性引入强彩色，优先调整灰阶透明度、密度范围、对比度和 brush 区间。
