# 621 MCF README

更新日期：2026-06-21

## 当前结论

本轮先 debug 了两个遗留问题：

1. 首页实时短片里的“片层坍缩”基本看不到。
2. 进入首页/探索后时间控制播放不了。

当前处理结果：

- “片层坍缩”已修复到可见状态。
- “进入数据探索后时间控制播放不了”在当前版本未复现；已验证进入探索后可以播放，step 会推进。
- 首页封面状态下时间控制本来是隐藏的，只有点“进入数据探索”后才应使用普通时间控制。

随后用户澄清：问题不是播放按钮完全失效，而是“数据探索内播放时，随着时间步变化不明显，像缺帧或渲染问题；拓扑不应该静止不变，而应该随着时间一起变化”。

针对澄清后的问题，当前已处理：

- 撤销了“播放时隐藏拓扑”的错误处理。
- 探索页拓扑保持默认显示。
- 自动播放每个时间步都会同步更新拓扑网络，不再只推进 slider 和体渲染纹理。
- 自动播放时持续预取后续全分辨率体数据，减少长期停留在低清 preview 路径的问题。
- 探索播放改成关键帧推进：约 `160ms` 一帧，每帧跨 `3` 个数据 step，接近手动拖条时的可见变化。
- 探索播放到 `99` 后停止，不再从 `99 -> 0` 循环跳变。
- 播放期间 ray-march 步进数从 `88` 提高到 `128`，减少播放时体渲染发糊、低分辨率感过强的问题。
- 首页短片仍覆盖 `0 -> 99` 全时间，但分为 5 个章节；现在所有章节都启用拓扑，让拓扑从初始微扰阶段就随时间演化。

## 本轮新增修复

### 1. 片层坍缩可见性

问题原因：

- sheet 阶段原本刷选 `25% - 75%` 密度区间。
- 当前黑白 transfer function 在这个中密度区间 alpha 很低，尤其 `q25/q50` 基本透明，`q75` 也只有很弱 alpha。
- 所以即使 story 切到 sheet brush，实时体渲染里片层几乎不可见。

修复方式：

- 在 `src/visualization/shaders.js` 增加 `uBrushBoost` uniform。
- 在 brush 生效时，用 `uBrushBoost` 提高刷选区域的 opacity，并轻微提高亮度。
- 在 `src/visualization/VolumeRenderer.js` 的 `setBrush(active, min, max, boost = 1)` 支持可选 boost。
- 在 `src/main.js` 的 story sheet 阶段单独设置：
  - `density: 1.30`
  - `brush: "sheet"`
  - `brushBoost: 5.5`
  - sheet 范围从 `25% - 75%` 扩到 `25% - 90%`

注意：

- 普通交互 brush 仍默认 `boost = 1`，不会整体改变用户探索时的刷选行为。
- 体渲染仍保持黑白灰，不引入强彩色。

验证证据：

- 浏览器运行到 `STAGE 02 / SHEETS` 后读取 uniform：
  - `density = 1.3`
  - `brushActive = true`
  - `brushBoost = 5.5`
  - `controls = false`
- 截图视觉确认片层结构明显可见。
- 截图中心区域亮度统计：
  - mean 约 `33.15`
  - p90 约 `66`
  - `亮度 > 35` 的像素约 `31.7%`
  - `亮度 > 55` 的像素约 `14.5%`

### 2. 时间控制播放

复现/验证方式：

- 打开 `http://127.0.0.1:5173/`
- 点击首页“进入数据探索”
- 点击左侧时间控制播放按钮

当前验证结果：

- 点击前：
  - step = `0`
  - play button = `▶`
  - button visible = `true`
  - controls = `true`
- 点击后约 750ms：
  - step = `9`
  - play button = `❚❚`
  - playTimer = `true`
  - loadState = `○ 预览(播放)`
  - controls = `true`

结论：

- 当前版本里，进入数据探索后普通时间播放是可用的。
- 如果用户说的是“首页封面状态下”的时间控制，那时普通时间控制被 story-mode 隐藏，是设计行为，不是播放按钮失效。

### 3. 探索播放变化不明显 / 拓扑未随时间变化

问题原因：

- 手动拖时间条会调用 `setStep()`，再进入 `applyStep(step, { fromPlayback: false })`。
- 这个路径会执行 `_updateNetworkLayer(step)`，也会触发当前 step 的全分辨率加载。
- 自动播放会调用 `applyStep(step, { fromPlayback: true })`。
- 旧逻辑里 `fromPlayback = true` 时跳过 `_updateNetworkLayer(step)`，因此拓扑几何会停留在旧 step，看起来“不合理地一直存在/不变”。
- 自动播放旧逻辑也只在手动路径里做预取，导致播放中更容易长期使用低清 preview 体纹理。
- 探索播放参数原本偏向流畅：`stepInterval = 78ms`、`playbackSteps = 88`。这会让时间推进偏快，同时体渲染采样偏粗，用户更容易感觉变化不明显。

修复方式：

- `src/main.js`
  - 撤销 `topologyBeforePlayback` 和播放时隐藏拓扑的逻辑。
  - `applyStep()` 中从 `if (!fromPlayback) this._updateNetworkLayer(step)` 改为：
    - 非播放路径照常更新拓扑；
    - 播放路径只要 `networkVisible = true`，也更新拓扑。
  - `prefetch()` 从只在非播放路径触发，改为播放路径也触发。
  - `play()` 开始时先预取后续 8 帧全分辨率体数据。
  - `stepInterval` 从 `78` 改为 `160`。
  - 新增 `playbackStride = 3`，探索播放每帧跨 3 个数据 step。
  - `_tick()` 从循环播放改成到最后一步 `99` 自动暂停。
  - `playbackSteps` 从 `88` 改为 `128`。
  - 短片章节 `STAGE 01` 和 `STAGE 02` 的 `topology` 从 `false` 改为 `true`。

验证状态：

- `npm run build` 已通过。
- 由于当前环境缺少可调用的 Playwright 浏览器二进制，且 in-app browser 控制接口这次连接失败，未完成自动截图像素差分验证。
- 需要在浏览器中人工复核：
  - 进入数据探索后，拓扑按钮应保持 active。
  - 中间体渲染上应默认显示拓扑线/点。
  - 点击播放时，时间步应按 `+3` 推进，到 `99` 后停止。
  - 播放期间拓扑形态应跟着 step 变化。
  - 手动拖动和自动播放在同一时间步附近看到的拓扑/体渲染差异应接近。
  - 首页短片从 `STAGE 01` 开始就应有拓扑，并随时间变化。

## 已确认的其他状态

- 固定 MP4/WebM 短片方案已取消。
- 源码和 dist 里已无：
  - `STORY_VIDEO`
  - `cinemaVideo`
  - `video-playing`
  - `cosmic-web-story`
  - `.mp4`
  - `.webm`
- `public/videos` 和 `dist/videos` 已清空/不存在。
- 当前服务为：
  - `http://127.0.0.1:5173/`
  - 监听进程：单个 5173 listener。
- `npm run build` 已通过。

## 当前重要文件

- `src/main.js`
  - 首页实时 story 流程、sheet 阶段参数、时间播放控制、story/explore 切换。

- `src/visualization/shaders.js`
  - 新增 brush boost 在体渲染 shader 中提升刷选区域可见性。

- `src/visualization/VolumeRenderer.js`
  - `setBrush()` 增加可选 boost 参数。

- `src/styles.css`
  - 首页黑白灰视觉、按钮克制样式、图表样式覆盖。

- `src/themes.js`
  - 体渲染灰阶主题；二维图保留低饱和指标色。

## 后续如仍有反馈，优先检查

1. 如果用户仍说“探索播放变化不明显”
   - 检查 `window.__app.stepInterval` 是否为 `160`。
   - 检查 `window.__app.playbackStride` 是否为 `3`。
   - 检查 `window.__app.playbackSteps` 是否为 `128`。
   - 检查播放时 `window.__app.renderer.uniforms.uSteps.value` 是否约为 `128`。
   - 检查播放时 `window.__app.currentNetwork.step` 是否随 `#timeSlider.value` 一起变化。
   - 检查播放到 `99` 后是否暂停，避免 `99 -> 0` 的首尾跳变被误解为最后几步突变。
   - 对比 step 0、20、40 的体渲染截图，判断是数据本身变化慢，还是 preview texture 没刷新。
   - 如果仍不明显，下一步可考虑给播放添加轻微时间差分增强或关键帧插值，但要避免破坏真实数据含义。

2. 如果用户仍说“中间拓扑不随时间变”
   - 检查 `window.__app.networkVisible`。
   - 检查 `window.__app.renderer.networkVisible`。
   - 检查 `window.__app.renderer.networkLines.visible` 和 `networkPoints.visible`。
   - 检查 `#btnTopology.classList.contains("active")`。
   - 检查 `window.__app.currentNetwork.step` 是否等于当前 slider step。

3. 如果用户仍说“时间控制播放不了”
   - 先确认用户是否已经点了“进入数据探索”。
   - 首页封面状态下普通时间控制不可见，这是当前设计。
   - 若进入探索后仍不动，检查：
     - `state.playing`
     - `window.__app.playTimer`
     - `#timeSlider.value`
     - `#btnPlay.textContent`
     - `#app.className`

4. 如果用户仍说“片层坍缩看不到”
   - 先确认浏览器刷新后加载的是当前 5173 服务。
   - 检查 sheet stage uniform：
     - `uBrushActive`
     - `uBrushBoost`
     - `uDensityScale`
     - `uBrushMin/uBrushMax`
   - 不建议恢复彩色体渲染；用户要求体渲染保持黑白参考风格。
